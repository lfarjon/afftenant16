import { DynamicBlockLoaderDirective } from './dynamic-block-loader.directive';

describe('DynamicBlockLoaderDirective', () => {
  it('should create an instance', () => {
    const directive = new DynamicBlockLoaderDirective();
    expect(directive).toBeTruthy();
  });
});
